---
title: "Kalibracja_Quarto"
format: html
---

# Ładowanie danych
```{r}
library(tidyverse)
library(openair)
```


```{r}
load("dane/ops.RData") 
```

```{r}
ops |> summary()
```


```{r}
ops |> is.na() |> colSums()
```

```{r}
ops |> glimpse()
```


```{r}
load("dane/ops.RData") ; ops <- ops |> na.omit()
load("dane/data_test.rdata")
```

```{r}
weryfikacja <- ops_data |> select(date:prec)
weryfikacja <- weryfikacja |> 
  left_join(bam, by = "date") |> 
  mutate(grimm_pm10 = bam_pm10) |> 
  select(-bam_pm10)
ops <- ops |> select(-ops_pm10, -pres_sea)

nazwy <- colnames(ops) |> dput()
weryfikacja <- weryfikacja |> 
  select(all_of(nazwy))

colnames(weryfikacja) == colnames(ops)
```

```

